# ValuationAgent 当前进展与金融模型接入报告

日期：2026-09-23
范围：A股非金融上市公司；银行、券商、保险及其他金融行业不在本项目研究范围内。

## 1. 当前结论

ValuationAgent 已形成可以运行、复核、追溯和继续扩展的 Agent 框架。CLI 与 Web 共用后端任务、模型、对话、版本和执行事件；LLM 负责理解意图、读取非结构化资料、选择工具与解释结果，确定性程序负责财务校验和估值计算。

金融小组模型已经从“参考方案”接入为默认的非金融行业正式计算插件。完整结构化输入可以依次运行行业参数、收入预测、EBIT、税、D&A、CapEx、营运资本、WACC、FCFF、三情景 DCF、相对估值、敏感性分析和方法比较。金融行业在参数查询和财务验证阶段被明确阻断。

本次已经完成**模型工程、真实数据/搜索适配、研究到估值自动交接和正式报告链路**。A股在线取数采用Tushare Pro点时接口，联网搜索采用Tavily，研究会话的已确认候选可严格转换并提交估值，同业筛选和PDF/Excel报告已接入。仍未完成的是金融小组金标准案例逐项对账、扫描件OCR、多业务分部预测和完整三表逐科目联动，因此正式使用前仍应由金融人员复核。

## 2. Agent 已实现的完整机制

当前运行流水线为：

```text
用户目标 / A股代码 / PDF / DOCX / Excel / 结构化数据
    ↓
LLM理解意图、读取资料、提出任务或字段候选
    ↓
用户确认关键口径与歧义
    ↓
资料快照与财务规则校验
    ↓
行业识别和版本化参数查询
    ↓
经营假设 → 现金流预测 → DCF / 相对估值
    ↓
敏感性分析 → 方法差异复核
    ↓
结构化结果、自然语言解释、来源和工具轨迹
    ↓
JSON复算包 / Excel审计底稿 / PDF正式报告
```

### 2.1 输入与对话

- CLI 和 Web 都支持模型配置、自然语言长对话、选项确认和自由文字修改。
- DeepSeek、OpenAI 及其他兼容 Chat Completions/function calling 的模型通过会话接入；API Key 只保存在当前进程内存，不写入任务库或报告。
- 研究会话支持文本型 PDF、DOCX、XLSX、CSV、JSON、TXT、MD 的真实解析。LLM 按需读取原文块，避免把整份年报一次性塞入上下文。
- 字段候选在确认前不会进入权威事实；原始值、年份、单位、口径、页码或单元格位置可以随候选保存。
- 字段、年份、单位或口径发生冲突时，Prompt 要求模型如实说明，提供互斥选项或允许用户自由输入，禁止静默猜测。

### 2.2 核心 Agent 能力

- 意图识别、任务候选、事实候选、资料缺口、长期记忆和近期对话采用分层上下文。
- 工具调用使用注册白名单和 Pydantic 参数校验；模型自由文本不能直接覆盖正式估值数字。
- 金融工具、资料工具和未来搜索工具通过统一 `ToolSpec` / `AgentToolProvider` 接口注册。
- 异常会保留当前进度并进入可恢复交互；复核、更正和假设修改创建新版本，旧版本保持不变。
- SQLite 保存任务、消息、来源、事件、工具输入输出、模型版本和哈希检查点。相同输入可以安全复用，相关输入变化后只重算受影响步骤。
- 工具执行过程可以在 CLI 与 Web 中查看，包含阶段、状态、耗时、缓存复用及产物 ID。

主要实现位置：

- `src/valuationagent/application/research.py`：资料研究 Agent、长对话、候选确认与容错。
- `src/valuationagent/application/runner.py`：估值任务、对话解释、版本更正和恢复。
- `src/valuationagent/workflow/graph.py`：正式估值流水线和工具事件。
- `src/valuationagent/core/documents.py`：文件解析和可定位原文块。
- `src/valuationagent/market/tushare.py`：A股点时取数、行业映射和同业筛选。
- `src/valuationagent/search/providers.py`：Tavily真实搜索、mock与未配置降级。
- `src/valuationagent/application/research_valuation.py`：确认候选到严格估值请求的交接。
- `src/valuationagent/application/reporting.py`：Excel审计底稿与PDF正式报告。
- `src/valuationagent/storage/sqlite.py`：任务、证据、消息、事件与检查点。
- `src/valuationagent/api/main.py`：Web API、模型会话和能力接口。
- `src/valuationagent/cli/` 与 `web/src/`：命令行和网页交互界面。

## 3. 金融资料采用规则

本次按“后发布、补全程度更高的文件优先”整合金融小组资料：

1. `所有的公司都使用近十年数据(1).docx` 明确了历史期政策：目标使用最近十年；不足十年时使用全部可得年度；收入 CAGR 的指数为 `1 / (观测数 - 1)`。
2. `全行业自动化估值模型_定稿流程总览.docx` 提供收入、营业成本费用和 EBIT 以前的主流程。
3. `DCF剩余模块建模说明_全行业版.docx`（2026-09-22）补全税、D&A、CapEx、ΔNWC、WACC、终值、股权价值桥接和敏感性分析，覆盖早期文件中“EBIT之后待补充”的状态。
4. 两份行业参数表中采用较新的 `行业参数数据库_v2_全行业(1).xlsx`。共享工作表与早期版本一致，新版本增加工资系数内容。

最新版参数表已经转为程序可读取的确定性快照：

- 参数版本：`2026-09-22-v2-normalized`
- 行业参数行：21个非金融行业/兜底类别
- 源文件 SHA-256：`C38DFE8E48BF135A9187FF238398CB4B4D6302418B8FC768AEC0110F1881B65D`
- 代码文件：`src/valuationagent/finance/data/industry_parameters_v2.json`
- 查询实现：`src/valuationagent/finance/industry.py`

源文件名、哈希、参数版本、行业行、数据等级和实际采用值会进入模型结果，避免运行时直接从工作簿模糊匹配或被 LLM 改写。

## 4. 金融模型接入状态

| 模块        | 当前状态    | 已实现口径                                           |
| --------- | ------- | ----------------------------------------------- |
| 适用性控制     | 已接入     | 仅非金融A股；金融行业明确阻断；行业缺失时不冒充正式模型                    |
| 行业参数库     | 已接入     | 精确/别名/有限模糊匹配；多匹配时要求确认；返回版本、哈希和数据等级              |
| 历史收入      | 已接入     | 最近10个连续年度；4—9年提示样本风险；少于4年且无手工路径时阻断              |
| 收入增长      | 已接入     | 年度增速、CAGR、A3、A5、历史位置投票、行业增长、生命周期修正、三情景两阶段收敛     |
| EBIT      | 已接入基础版  | 收入 × EBIT率；利润率向历史P25/中位数/P75收敛；已有科目按收入比例保留明细轨迹  |
| 所得税       | 已接入     | 当前实际税率按政策期限向可持续税率过渡；允许用户覆盖终值税率                  |
| D&A       | 已接入并可降级 | 明细足够时采用资产滚动；否则采用收入比例法，并记录降级原因                   |
| CapEx     | 已接入     | 当前 CapEx/收入逐步向稳态 D&A/收入比例收敛                     |
| ΔNWC      | 已接入并可降级 | 优先经营营运资本/收入；其次由最近一期变化反推；再退化为比例法                 |
| WACC      | 已接入     | CAPM权益成本 + 税后债务成本，按市值/权益和有息负债加权；缺市值时警告并暂按100%股权 |
| FCFF      | 已接入     | `NOPAT + D&A - ΔNWC - CapEx`                    |
| DCF       | 已接入     | 悲观/基准/乐观三情景，10年期末折现，Gordon终值，企业价值到股权价值桥接，每股价值区间 |
| 终值校验      | 已接入     | `g < WACC`、终值占比、隐含退出倍数和无效情景提示                   |
| 敏感性分析     | 已接入     | 3×3 WACC × g 网格；无效组合明确标记                        |
| 相对估值      | 已接入计算   | P/E、P/S、EV/EBITDA；无效利润或样本不足时返回不适用               |
| 多模型验证     | 已接入     | DCF与相对估值独立展示，比较中枢差异；不把区间交集伪造为新推荐区间              |
| Agent金融工具 | 已接入     | 查询行业参数、检查模型适用范围；调用写入统一工具轨迹                      |
| A股在线取数    | 已接入     | Tushare Pro官方JSON接口；按估值日截断披露日和行情日，最多最近10个完整年报期  |
| 联网搜索      | 已接入     | Tavily真实搜索；保存URL、域名、发布日期、截止日、查询和供应商版本为候选证据      |
| 可比公司筛选    | 已接入     | 同供应商行业、正常上市、非ST、非金融；规模/增长/EBIT率排序；EV扣除货币资金      |
| 研究自动交接    | 已接入     | 已确认、标准字段、完整期间/单位/合并口径才可形成快照；歧义或缺失阻断并提示          |
| 正式报告      | 已接入     | JSON、Excel多工作表审计底稿、PDF两页以上报告；Web与CLI均可导出        |

主要实现位置：

- `src/valuationagent/finance/team_model.py`：EBIT之后的FCFF、WACC、DCF、相对估值和敏感性。
- `src/valuationagent/finance/revenue.py`：金融小组十年收入预测模型。
- `src/valuationagent/finance/industry.py`：行业识别、金融行业排除和参数来源。
- `src/valuationagent/finance/tools.py`：Agent可调用的金融查询工具。
- `src/valuationagent/finance/factory.py`：默认正式模型和参考模型显式切换。
- `src/valuationagent/schemas/models.py`：税率、WACC组成、三情景、逐年计算方法和相对估值结果契约。

## 5. 对原方案中模糊或冲突处的处理

以下内容没有静默猜测，均采用显式、可追溯的工程政策：

1. **衰减期 `T` 等于预测期 `N`**：原公式在终点无法完全收敛。程序使用 `T_eff = min(T, N - 1)`，保证最后一年等于终值增长率，并在 `model_decisions` 中保存原参数和处理结果。
2. **全球行业增速与永续增长率的含义冲突**：行业表中的全球增速只作为有限预测期合理性检查；永续增长率保持为独立长期稳态假设，避免把较高行业增速无限外推。
3. **DCF与相对估值区间如何合并未定**：当前不机械求交集或平均。两类方法分别输出，只计算中枢差异并提示复核方向。
4. **缺少行业的旧请求**：为了兼容已存在的任务，进入带明确警告的参考模型路径；补充非金融行业后才启用金融小组模型。结果中的 `model_version` 显示实际计算引擎。
5. **折旧摊销明细不足**：不伪造资产明细，降级为收入比例法，并在年度 `calculation_methods` 与任务警告中记录。
6. **收入历史年份缺口**：不插值、不填零，直接阻断并要求补资料或手工给定可解释的预测路径。

## 6. 可追溯性和可信控制

- 行业参数由版本化注册表确定，不由 LLM 直接生成。
- 每年预测结果保存收入增速、EBIT率、税率、NOPAT、D&A、CapEx、ΔNWC、FCFF和各字段计算方法。
- 假设结果保存三情景路径、WACC组成、行业参数、来源说明和模型决策。
- DCF结果保存显性期现值、终值、终值现值、终值占比、隐含退出倍数、股权桥接和情景每股价值。
- 工作流保存每次工具的输入哈希、输出、模型版本、事件序号和缓存来源。
- 用户修改假设会创建新版本；旧结果、旧输入和旧工具产物不会被覆盖。
- LLM负责提出、选择和解释，正式数值由确定性模型生成，报告层不会接受模型自由文本改写计算结果。
